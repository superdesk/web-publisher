{% trans_default_domain 'SWPUserBundle' %}

{{ form_start(form, { 'action': path('swp_user_profile_edit'), 'attr': { 'class': 'swp_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
