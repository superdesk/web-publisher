{% trans_default_domain 'SWPUserBundle' %}

{{ form_start(form, { 'action': path('swp_user_change_password'), 'attr': { 'class': 'swp_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
{{ form_end(form) }}
