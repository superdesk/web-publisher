framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        # failure_transport: failed

        transports:
            sync_export_analytics: "sync://"

        routing:
            'SWP\Bundle\CoreBundle\AnalyticsExport\ExportAnalytics': sync_export_analytics
