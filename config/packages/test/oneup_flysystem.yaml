oneup_flysystem:
    adapters:
        filesystem_adapter:
            local:
                directory: "%kernel.cache_dir%/uploads/"
        local_themes_adapter:
            local:
                directory: "%kernel.project_dir%/src/SWP/Bundle/CoreBundle/Tests/Fixtures/themes"
