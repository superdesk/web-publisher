proxy_cache_path /tmp levels=1:2 keys_zone=cache:100m inactive=60m use_temp_path=off;
proxy_cache_valid 200 301 302 10s;
proxy_cache_use_stale updating error timeout invalid_header http_500 http_503;
proxy_cache_background_update on;
proxy_cache_lock on;

map $http_user_agent $ua_device {
    default 'desktop';
    ~*(android|bb\d+|meego).+mobile|avantgo|bada\/|blackberry|blazer|compal|elaine|fennec|hiptop|iemobile|ip(hone|od)|iris|kindle|lge\ |maemo|midp|mmp|mobile.+firefox|netfront|opera\ m(ob|in)i|palm(\ os)?|phone|p(ixi|re)\/|plucker|pocket|psp|series(4|6)0|symbian|treo|up\.(browser|link)|vodafone|wap|windows\ ce|xda|xiino/i 'mobile';
    ~*android|ipad|playbook|silk/i 'tablet';
}
proxy_cache_key "$scheme$request_method$host$request_uri$ua_device";

# generify backend selection
#map $http_host $backend_host {
#    default         'publisher.cloud.funkedigital.de';
#    www.wmn.de      'publisher.cloud.funkedigital.de';
#    origin.wmn.de   'publisher.cloud.funkedigital.de';
#    stage.wmn.de    'publisher-stage.cloud.funkedigital.de';
#}

# redirect to www.wmn.de
server {
    listen 80;
    server_name wmn.de;
    location / {
        return 301 https://www.wmn.de$request_uri;
    }
}

# reverse proxy-cache for wmn.de
server {
    listen 8080;
    server_name _;

    location / {
        sub_filter_once off;
        sub_filter_types text/css txt/xml application/xml application/rss+xml;
        sub_filter '<head>' '<head><link rel="preconnect" href="https://mcdp-nydc1.outbrain.com"><link rel="preconnect" href="https://track.adform.net"><link rel="preconnect" href="https://log.outbrainimg.com">';
        # workaround for subs_filter solution
        sub_filter '<img src="/uploads/swp/sxl34v/media/' '<img src="/size/740x555/media/';
        sub_filter '<amp-img src="https://publisher.cloud.funkedigital.de/uploads/swp/sxl34v/media/' '<amp-img src="/size/740x555/media/';
        sub_filter '<amp-img src="/uploads/swp/sxl34v/media/' '<amp-img src="/size/740x555/media/';
        sub_filter '<meta name="robots" content="noindex">' '';

        proxy_cache cache;
        proxy_set_header Accept-Encoding "";
        proxy_set_header Host $host;
        proxy_pass http://unix:/var/run/nginx.sock;
    }
}

# publisher backend
server {
    listen 80;
    listen unix:/var/run/nginx.sock;
    server_name _;
    root /var/www/publisher/public;

    location / {
        # redirect requests from google to publisher
#        if ($http_referer ~ "https://www.google.(de|com)"){
            # FIXME remove referer header to avoid looping?
#            return 301 https://www.wmn.de$request_uri;
#        }

        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/size/(?<width>\d+)x(?<height>\d+)/media/(?<name>.*)$ {
        image_filter resize $width $height;
        image_filter_buffer 50M;
        alias /var/www/publisher/public/uploads/swp/sxl34v/media/$name;
    }

    location ~ ^/crop/(?<width>\d+)x(?<height>\d+)/media/(?<name>.*)$ {
        image_filter crop $width $height;
        image_filter_buffer 50M;
        alias /var/www/publisher/public/uploads/swp/sxl34v/media/$name;
    }

    location /media/ {
        alias /var/www/publisher/public/uploads/swp/sxl34v/media/;
    }

    # TODO use assets from FD-Cloud
    location ~ ^/size/(?<width>\d+)x(?<height>\d+)/assets/(?<name>.*)$ {
        proxy_set_header Host fdl-superdesk-assets.s3-website-eu-west-1.amazonaws.com;
        proxy_set_header Accept-Encoding "";
        proxy_pass_request_headers off;
        proxy_pass http://fdl-superdesk-assets.s3-website-eu-west-1.amazonaws.com;
        image_filter resize $width $height;
        image_filter_buffer 20M;
    }

    location /assets/ {
        proxy_set_header Host fdl-superdesk-assets.s3-website-eu-west-1.amazonaws.com;
        proxy_set_header Accept-Encoding "";
        proxy_pass http://fdl-superdesk-assets.s3-website-eu-west-1.amazonaws.com;
    }

    location /ads.txt {
        proxy_set_header Host ads.funkedigital.de;
        proxy_set_header Accept-Encoding "";
        proxy_pass http://ads.funkedigital.de;
    }

    location ~ ^/index\.php(/|$) {

        # workaround for tags in amp (amp-converter strips them off)
        sub_filter '<!--amp-consent'  '<amp-consent';
        sub_filter '</amp-consent-->'  '</amp-consent>';
        sub_filter '<!--amp-fx-flying-carpet'  '<aamp-fx-flying-carpet';
        sub_filter '</amp-fx-flying-carpet-->'  '</amp-fx-flying-carpet>';
        sub_filter '<!--form'  '<form';
        sub_filter '</form-->'  '</form>';
        sub_filter '<!--link rel="manifest"'  '<link rel="manifest"';
        sub_filter '/manifest.json"-->'  '/manifest.json">';
        sub_filter '<!--script'  '<script';
        sub_filter '</script-->'  '</script>';
        sub_filter_once off;
        #sub_filter_last_modified on;

        fastcgi_pass php:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        fastcgi_param QUERY_STRING $query_string;
        fastcgi_param REQUEST_METHOD $request_method;
        fastcgi_param CONTENT_TYPE $content_type;
        fastcgi_param CONTENT_LENGTH $content_length;
        fastcgi_param SCRIPT_NAME $fastcgi_script_name;
        fastcgi_param REQUEST_URI $request_uri;
        fastcgi_param DOCUMENT_URI $document_uri;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param SERVER_PROTOCOL $server_protocol;
        fastcgi_param HTTPS $https if_not_empty;
        fastcgi_param GATEWAY_INTERFACE CGI/1.1;
        fastcgi_param SERVER_SOFTWARE nginx/$nginx_version;
        fastcgi_param REMOTE_ADDR $remote_addr;
        fastcgi_param REMOTE_PORT $remote_port;
        fastcgi_param SERVER_ADDR $server_addr;
        fastcgi_param SERVER_PORT $server_port;
        fastcgi_param SERVER_NAME $server_name;
    }

    error_log /var/log/nginx/publisher_error.log;
    access_log /var/log/nginx/publisher_access.log;
}
